#!/system/bin/sh
#load environment file
set -a && . "${RCLONEDIR:-/data/adb/modules/rclone}/env" && set +a

rclone rcd \
  --rc-web-gui \
  --rc-web-gui-no-open-browser \
  --rc-web-gui-update \
  "$@"
